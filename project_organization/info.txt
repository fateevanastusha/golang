GOROOT — 
это переменная, указывающая, где лежит, собственно, вся бинарная сборка Go и исходные коды (сам GO).
Раньше (до Go 1.0) эта переменная была нужна — её использовали скрипты сборки, равно как и GOARCH и 
GOOS. Но после Go 1.0 немного изменилась внутренняя логика работы go tool и сейчас значение GOROOT 
хардкодится на этапе сборки или инсталляции. То есть, go — дефолтно проинсталлированный — знает это 
значение и так.

go env GOROOT - посмотреть значение
Пример: /usr/local/go


GOPATH - 
очень важна и нужна и её нужно установить обязательно, только один раз. Это ваш workspace (рабочая
директория), где будет лежать весь твой Go-код, скаченные библиотеки, и собранные бинарники.

echo 'export GOPATH=$HOME/go' >> ~/.bash_profile - выставить переменную

~/go/
├── bin/       
├── pkg/        
└── src/  

1) bin
- Здесь будут собранные .exe/.out программы (программа - это где есть func main(). При компиляции через 
go install, компилятор создает исполняемый файл и кладет его сюда).
2) pkg
- Здесь бинарники библиотек (кэш для ускорения компиляции) (библиотека - это package без func main(), 
при компиляции go install создается .a архив и кладет его сюда. Это бинарный объект для переиспользования, 
не запускается как программа).
3) src
- исходный твой код и код сторонних библиотек

go get github.com/someuser/somelib - скачает в ~/go/src/github.com/someuser/somelib
go install github.com/myuser/hello - скомпилирует и положит бинарник в: ~/go/bin/hello

PATH - 
рано или поздно придется пользоваться какими-то Go-программами, которые будут лежать в GOPATH/bin. 
Чтобы их без лишних телодвижений использовать, добавьте к PATH директорию $GOPATH/bin:
export PATH=$PATH:$GOPATH/bin
Получается, можно запустить программу прям из терминала (ollama, например).




Структура workspace

bin/
    hello                 # программа hello
pkg/
    linux_amd64/          # каталог этого уровня определяет ось и архитектуру для бинарных файлов
        github.com/user/
            stringutil.a  # объектный бинарный файл пакета stringutil
src/
    github.com/user/
        hello/
            hello.go      # исходный код программы hello
        stringutil/
            reverse.go    # исходный код библиотеки stringutil



КОМАНДЫ
1) 
go install - собирает и инсталлирует бинарник именно в директорию в переменной PATH.

2) 
go get github.com/someuser/somelib - скачать библиотеку

3)
go mod download - скачать все зависимости из проекта из go.mod

4)
go mod tidy - скачать все зависимости из проекта из go.mod, при этом еще удалить неиспользуемые и 
в соответствии с этим обновить go.sum

5) 
go mod init {{name_of_mod}} - создать модуль с именем. Создаст файл go.mod 

6)
go get -u - получить последний минорный патч или релиз (т.е. команда обновит с 1.0.0 до, скажем, 
1.0.1 или до 1.1.0, если такая версия доступна)

7) 
go get -u=patch - получить последнюю патч-версию (т.е. пакет обновится до 1.0.1, но не до 1.1.0)

8)
go get package@version - обновиться до конкретной версии (например, github.com/robteix/testmod@v1.0.1)

Из-за того, что мажорные версии могут ломать обратную совместимость, в этом списке нет прямого
способа обновить мажорную версию. С точки зрения Go модулей, мажорная версия — это совершенно другой 
пакет. Версии 2 и более должны сменить путь импорта. Теперь это разные библиотеки.
module github.com/robteix/testmod/v2 - добавляем v2
Это значит, что у нас может быть две несовместимые версии в одном бинарнике:
import (
    "fmt"
    "github.com/robteix/testmod"
    testmodML "github.com/robteix/testmod/v2"
)
func main() {
    fmt.Println(testmod.Hi("Roberto"))
    g, err := testmodML.Hi("Roberto", "pt")
    if err != nil {
        panic(err)
    }
    fmt.Println(g)
}

ВЕРСИОНИРОВАНИЕ:
   ↑   ↑   ↑
  2.   5.   1
  │    │    └── patch (патч)
  │    └────── minor (минорная)
  └─────────── major (мажорная)

🔹 Патч-версия (patch)
	•	Для исправлений багов без добавления нового функционала.
	•	Пример: 1.2.0 → 1.2.1

🔹 Минорная версия (minor)
	•	Меняется, когда добавляют новые фичи, не ломая совместимость.
	•	Всё старое продолжает работать.
	•	Пример: 1.2.0 → 1.3.0
📌 Пример: добавили новый метод, но старые остались.

🔹 Мажорная версия (major)
	•	Изменяется, когда есть обратимо несовместимые изменения.
	•	Например: API ломается, удалили старый метод, поменяли поведение.
	•	Пример: 1.2.0 → 2.0.0
📌 Пример: библиотека больше не поддерживает старый способ и требует от тебя переписать код.


go.sum - содержит хэши пакетов, чтобы проверять правильность версии и файлов: 
```
github.com/robteix/testmod v1.0.0 h1:9EdH0EArQ/rkpss9Tj8gUnwx3w5p0jkzJrd5tRAhxnA=
github.com/robteix/testmod v1.0.0/go.mod h1:UVhi5McON9ZLc5kl5iN2bTXlL6ylcxE9VInV71RrlO8=
```